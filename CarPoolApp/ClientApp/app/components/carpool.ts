export class Carpool {
    constructor(
        driver: string,
        seats: number,
        carDescription: string,
        startLocation: string,
        endLocation: string,
        weekDays: string[],
        time: number,
        additionalDetails: string
    ) { }
}
